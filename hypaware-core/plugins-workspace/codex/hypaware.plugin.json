{
  "schema_version": 1,
  "name": "@hypaware/codex",
  "version": "2.0.0",
  "description": "Codex CLI and Desktop capture from local rollout files. Inference connects directly to the provider by default; gateway capture is an explicit option. Includes query and privacy skills.",
  "hypaware_api": "^1.0.0",
  "runtime": "node",
  "node_engine": ">=20",
  "entrypoint": "./src/index.js",
  "permissions": [
    "read_home",
    "write_home",
    "read_state",
    "write_codex_settings"
  ],
  "requires": {
    "capabilities": {
      "hypaware.ai-gateway": "^2.0.0"
    }
  },
  "contributes": {
    "commands": [
      { "name": "codex-hook classify-cwd", "summary": "Internal Codex hook: nag to classify an unclassified folder on an enrolled machine", "hidden": true }
    ],
    "client": {
      "name": "codex",
      "skill_dir": ".codex/skills",
      "attach_probe": {
        "format": "toml",
        "settings_file": ".codex/config.toml",
        "marker_header": "[model_providers.hypaware]"
      },
      "launch": { "bin": "codex", "args": ["{prompt}"], "label": "Codex" }
    },
    "picker": [
      {
        "name": "codex",
        "label": "Codex",
        "summary": "Records Codex CLI and Desktop from local session files, and adds query skills. Removes an old managed gateway route. Full tool definitions are unavailable. Storage opens a local port.",
        "detect": { "settings_file": ".codex/config.toml" },
        "compose": {
          "plugin": { "name": "@hypaware/codex", "config": {} },
          "requires_gateway": true
        }
      }
    ],
    "skills": [
      { "name": "hypaware-query", "clients": ["codex"] },
      { "name": "hypaware-reference", "clients": ["codex"] },
      { "name": "hypaware-privacy", "clients": ["codex"] }
    ],
    "config_sections": [
      {
        "section": "codex",
        "summary": "Codex capture_mode (transcript by default, or gateway) and backfill policy (on_join, window_days, sweep_cron)."
      }
    ]
  }
}
