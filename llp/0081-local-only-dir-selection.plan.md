# LLP 0081: local-only directory selection — implementation plan

**Type:** plan
**Status:** Active
**Systems:** Sinks, CLI, Onboarding, Config
**Generated-by:** neutral
**Related:** LLP 0080, LLP 0069
**Date:** 2026-07-06

> Implementation steps for the `local-only` directory selection designed in
> [LLP 0080](./0080-local-only-dir-selection.design.md) (spec
> [LLP 0069](./0069-local-only-dir-selection.spec.md)). Small,
> independently-mergeable tasks: the machine-local store first, then the
> resolver, then the export-seam enforcement (the core) and the attended
> surfaces in parallel, closed by a hermetic smoke. No task touches the
> capture path, the cache schema, or the gateway — enforcement is
> export-read + resolver only (LLP 0070).

## Tasks
- id: T1  branch: task/local-only-dir-selection/T1  deps: []            complexity: 2  -- Machine-local list store `src/core/usage-policy/local_only.js` (`@ref LLP 0071 [implements]`): `localOnlyListPath(stateDir)` → `<stateDir>/usage-policy/local-only.json`; `readLocalOnlyDirs` (missing ⇒ `[]`; unparseable ⇒ throw with `error_kind: 'local_only_list_unreadable'` — the LLP 0080 #fail-safe); `writeLocalOnlyDirs` (normalize/dedupe/sort absolute paths, mkdir -p, tmp+rename atomic write); export from the usage-policy barrel. Tests: round-trip, missing⇒[], corrupt⇒throw, dedupe/normalize, atomic replace.
- id: T2  branch: task/local-only-dir-selection/T2  deps: [T1]          complexity: 2  -- Resolver second source (`@ref LLP 0070#resolver [implements]`, `@ref LLP 0071 [implements]`): `createUsagePolicyResolver` gains optional `localOnlyListPath`; `resolve(cwd)` merges `.hypignore` ancestor walk with segment-aware list membership (equal-or-descendant, LLP 0049 #scope) and returns most-restrictive (`ignore` > `local-only` > `full`); list parse memoized with the existing 5s TTL; list-governed result `{ class: 'local-only', governedBy: <list path>, declared: 'local-only' }`. `format.js` `IMPLEMENTED` gains `'local-only'` (dotfile token no longer clamps to `ignore`). Tests: equal/descendant/sibling-prefix (`/a/bc` vs `/a/b`), most-restrictive vs dotfile, TTL re-read of an edited list, dotfile `local-only` token honored, no-list resolver unchanged.
- id: T3  branch: task/local-only-dir-selection/T3  deps: [T2]          complexity: 4  -- Export-seam enforcement, drop-but-advance (the core; `@ref LLP 0070#enforce [implements]` / `#incremental [constrained-by]`): `createQueryStorageService` gains optional `usagePolicyResolver` (activation.js:55 defaults it from `localOnlyListPath(stateDir)`); `readRowsSince` yields `{ after, dropped: true }` (no payload) for rows whose non-empty `cwd` resolves to class ≠ `full`, passes cwd-less rows untouched, and lets a corrupt-list throw fail the partition read; kernel contract updated (`hypaware-plugin-kernel-types.d.ts:1172` discriminated union). Consumers updated in the same change: `central/src/sink.js` `forwardPartition` advances `lastAfter` on drops, counts `droppedRowCount`, widens the watermark gate to `shipped>0 || dropped>0` (ship-first/advance-second preserved); `src/core/sinks/incremental.js` `openIncrementalRows` skips drops in `rows()`, advances `lastAfter` incl. trailing drops, exposes `droppedRowCount`, peeks past leading drops for `empty`; s3 + local-fs write the watermark on an empty-but-dropped tick. Telemetry `usage_policy.export_drop` (hashed cwds). Tests: cache-but-never-forward for a local-only cwd (rows land in cache, absent from forward payload), drop-only tick checkpoints, un-excluded dir not re-sent past a checkpoint, failed chunk still never checkpoints, cwd-less datasets unaffected, corrupt list ⇒ tick fails with watermark untouched.
- id: T4  branch: task/local-only-dir-selection/T4  deps: [T3]          complexity: 2  -- format-iceberg reroute (`@ref LLP 0070#why-export [implements]`): `table-format.js` `openRows` reads via `storage.readRowsSince` full-scan (`since` absent, `includeLegacy: true`), skipping drop-only entries and discarding `after` (snapshot/marker-based, no cursor) — the last export path off the filtered seam. Tests: committed snapshot excludes local-only rows; unaffected when no list.
- id: T5  branch: task/local-only-dir-selection/T5  deps: [T1]          complexity: 3  -- Enumeration + picker in new `src/core/commands/local_only.js`: `listCapturedDirectories` (`@ref LLP 0069#enumerate [implements]`) — one `executeQuerySql` (`refresh: 'never'`) `SELECT cwd, repo_root, COUNT(*) AS rows, MAX(date) AS last_seen ... GROUP BY cwd, repo_root ORDER BY last_seen DESC`, collapsed per-cwd, best-effort (error ⇒ null ⇒ picker skipped, never blocks login); `runLocalOnlyPicker` (`@ref LLP 0072 [implements]`) — stdin+stderr TTY gate, existing `src/core/cli/tui` `multiselect` rendered on stderr (path label; repo_root/count/last-seen summary; cap 50 + "…and N more" hint), Ctrl-C/EOF/`PromptCancelledError`/empty ⇒ zero exclusions and proceed, existing list entries pre-checked (editor semantics; non-candidate entries preserved), persist via `writeLocalOnlyDirs`, never-silent "withholding N…" line, `local_only.picker_result` telemetry. Tests: injected tui io, cancel⇒proceed-with-zero, non-TTY skip+hint, empty candidates skip, pre-check + preserve semantics.
- id: T6  branch: task/local-only-dir-selection/T6  deps: [T2]          complexity: 2  -- Durable CLI authoring (`@ref LLP 0072#cli [implements]`): `parseIgnoreArgs` gains `--local-only`; `hyp ignore --local-only [path]` adds the resolved target (repo-root default, explicit path wins) to the list, idempotent, never writes into a repo, accepts nonexistent/non-repo paths (R4); `hyp unignore --local-only [path]` removes every governing entry (equal-or-ancestor), idempotent; `hyp ignore --check` reports the extended resolver's class + governing source (dotfile or list) + existing residual count; update verb usage strings in `core_commands.js`. Tests: add/remove idempotency, ancestor-governed no-op, --check for both sources.
- id: T7  branch: task/local-only-dir-selection/T7  deps: [T5]          complexity: 2  -- Login wiring (`@ref LLP 0069#trigger [implements]`, `@ref LLP 0072 [implements]`): call `runLocalOnlyPicker` in `runBrowserLogin` (`src/core/cli/remote_commands.js`) after `seed(...)` succeeds and before the `seeded.length === 0` fork — past the `--no-forward`/no-gateway early-returns, strictly before `enrollCentralSink` (R6); non-cancellation picker errors caught ⇒ warn + zero exclusions (never breaks enrollment). Tests: list persisted before `enrollCentralSink` is invoked (ordering assertion), `--no-forward`/query-only logins never invoke the picker, non-TTY login completes unchanged, re-login (already-enrolled) path shows the picker.
- id: T8  branch: task/local-only-dir-selection/T8  deps: [T1]          complexity: 1  -- `hyp status` surface (R9, `@ref LLP 0069#requirements [implements]`): `collectHypAwareStatus` gains best-effort `usagePolicy.localOnlyDirCount` (corrupt list ⇒ diagnostic); text render `local-only: withholding N directories from forwarding (recorded locally)` when N > 0, count in `--json`. Tests: zero/none hidden, N>0 rendered, corrupt-list diagnostic.
- id: T9  branch: task/local-only-dir-selection/T9  deps: [T3, T6, T7]  complexity: 3  -- Hermetic smoke `hypaware-core/smoke/flows/local_only_export_withhold.js`: seed rows from two cwds, mark one local-only via `hyp ignore --local-only`, run the forward/export tick; assert clean-cwd rows export, excluded-cwd rows stay locally queryable and reach no sink, watermark advances past them (second tick reads zero rows), `usage_policy.export_drop` emitted, `hyp status` shows the count (stable `DEV_RUN_ID`/`smoke_name`/`smoke_step`). Register in the smoke flow index.

## Notes

- The design (LLP 0080) is neutral-minted `Status: Active`; merging the change
  set ships it — no status-flip task.
- T3 is the privacy-critical core and the only complexity-4 task: one seam,
  three consumer call sites, and the cursor invariant, which must land
  together because the `readRowsSince` yield type is load-bearing.
- T4–T8 are parallel after their deps; T9 closes the loop end-to-end.
- No task touches the adapters, the gateway, settlement, or the cache schema
  (LLP 0070: no LLP 0029 migration, no capture-time marker).
