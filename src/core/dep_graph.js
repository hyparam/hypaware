// @ts-check

// Phase 0 skeleton placeholder. Topological resolution over `requires`
// (plugins + capabilities) arrives in Phase 1
// (see hypaware-implementation-plan.md §Phase 1). The contract is
// declared in dep_graph.d.ts.

export {}
